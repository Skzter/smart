package entity

// RunTestResponse represents the outcome of a run request. Typical
// fields include logs, status or any structured result; add these
// when the executor returns them.
type RunTestResponse struct {
}
